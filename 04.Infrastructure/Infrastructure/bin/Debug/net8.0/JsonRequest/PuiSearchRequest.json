
{
    "model": "{{entity}}",
    "page": "{{page}}",
    "rows": "{{rows}}",
    "filter": {
      "groupOp": "and",
      "rules": [
        {
          "field": "{{field}}",
          "op": "eq",
          "data": "{{value}}"
        }
      ]
    },
    "order": [{
      "column": "name",
      "direction": "asc"
      }
    ]
}